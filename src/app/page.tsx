'use client';

import React from 'react';
import App from '@/components/App';

/**
 * CloudForge — Home page.
 * The App component is a client-side SPA shell (landing, dashboard,
 * workspace, marketplace, academy…). SSR-safe: all browser-only APIs
 * inside App are guarded.
 */
export default function Page() {
  return <App />;
}
